{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Inscryption",
            "type": "shell",
            "command": "make",
            "args": ["game"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": false
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}